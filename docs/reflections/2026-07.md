
## 2026-07-09 - glasshull (#6214 operator-decision: live CRM config + Power Automate notify flow)
Findings (all applied -> auto-memory):
- PA "Add a new row" cannot set email from/to PartyList (only sender/torecipients STRING caches, which don't send) -> classic workflow Send Email for a CRM-tracked email from a queue [reference: power-automate-cant-set-email-parties]
- Authored a full deploy doc + guided ~90% of a build on an UNVERIFIED connector capability -> spike-verify the one non-obvious capability first [feedback: verify-connector-capability-before-build]
- Cloud-flow clientdata is editable via PATCH workflows(id) {clientdata}; constraints: valid connector params only (unknown->InvalidOpenApiFlow) + no open unpublished draft (->"unpublished active row") [reference: scripting-power-automate-flow-clientdata]
- (housekeeping) updated booking-flow-alignment-status: CRM live-config DONE (formula col + 2 subgrid views + Approve/Reject buttons), notify BLOCKED pending classic-wf-vs-O365
Routes: 3 new memories + 1 memory update + 4 MEMORY.md pointers. No Route A (no owned skill guided this wrong) / B / C.
Sources: MS Learn "Use relationships to modify rows" (activity parties = OData ID / array-mode); live EntityDefinitions metadata (email from/to=PartyList, sender/torecipients=String; ctg_cargodetail not an email Regarding target).
## 2026-07-10 — GlassHull #6215 (Booking Flow Step 3: VIN Collection) — grill-then-plan
- **Applied (Route D, memory update):** load-bearing platform-capability assumption went unverified until the user prompted a PoC — the VIN email was specced as regarding `ctg_cargodetail`, but a live check found the entity wasn't activity-enabled / not a valid `email.regardingobjectid` target (design was infeasible; fixed by enabling Activities). Generalized `verify-connector-capability-before-build` beyond connectors to Dataverse metadata behaviors; verify DURING grilling, before the spec/ADR.
- **Skipped (proposed, not applied):** (1) `mobile-app` write-guard false-positives on drive-letter case (c: vs C:) — ~8 wasted retries; proposed new memory `mobile-app-writeguard-drive-case`. (2) jargon-dense grilling questions drew two "I don't understand" replies; proposed plain-language clause on `prefer-prose-recommendation-over-question-grid`.
- **Source:** in-session live metadata PoC (glasshulldemo EntityDefinitions — email regardingobjectid Targets 60→61 after enabling Activities).

## 2026-07-10 — GlassHull Feature 6215 (VIN Collection)
Session: scrutinize x2 -> fix-all -> verify (BE 358/358, FE green) -> generating-test-cases (21 cases) -> reflect.
Findings:
- [D applied] Write-endpoint per-record auth: GlassHull has no write-scope primitive; scoped WRITE endpoints reuse ResolveBookingAccess (read-level reachability) + [GlassHullScope] (GlassHullScopeFilter is HTTP-method-agnostic). Updated memory glasshull-quote-access-and-scope-gotcha (fact #3). Source: scrutiny fix commit 02aec05; GlassHullScopeFilter.cs:32-88.
- [A applied] generating-test-cases render_xlsx.py: the guess-word flag now also scans the Test Data column (SKILL Step 8 requires it; the script previously checked only Expected + Steps). dev-workflows 0.23.0 -> 0.23.1; needs cache resync + restart.
Skipped: write-guard->Bash workaround (already in wiki-write-guard-blocks-vault memory), replace-all copy fix (one-off content), scrutinize->fix->re-scrutinize->test-cases chaining (user-driven, worked fine).

## 2026-07-10 — glasshull (daily wrap /reflect)
- Finding: Tribletext daily entries condensed by user 4->2 lines -> APPLIED (Route D memory: tribletext-entry-length-preference).
- Session otherwise trivial/conversational (daily wrap -> invoice-generator -> management-talk); no skill failures, no durable friction.
- Sources: none (local-only).

## 2026-07-11 — menunest (auth: "re-login every ~1h")
- F1 [applied · Route A · dev-workflows/debug-mantra v0.23.3]: added production telemetry / log stores as fail-path tactic 4 — run normal tactics FIRST, then ASK the user before querying log stores (user refined: telemetry is not a proactive first move).
- F2 [applied · Route C · menunest/CLAUDE.md]: menunest App Insights is workspace-based — classic `az monitor app-insights query` returns `[]`; must query the Log Analytics workspace (customerId 587ba1f6-…) with `App*` schema tables via `az monitor log-analytics query`.
- F3 [applied · Route D · memory feedback]: Ultracode's "always Workflow" must not override process-skill order — a fix with an unverified cause routes to debug-mantra FIRST, before any Workflow/inline investigation.
- Skipped: none.
- Sources: live App Insights / Log Analytics queries this session (workspace 587ba1f6-9c1c-4c74-9f0e-4581f3f765a2).
## 2026-07-12 — menunest (Trips: current-time-start timezone fix)

Flow: debug-mantra → grill-then-plan → writing-plans → subagent-driven-development (4 tasks + fixes) → 2× scrutinize → reflect. Bug: a day flagged "always use current time" showed the server UTC clock (GetItineraryHandler used DateTime.Now; Azure App Service is UTC) instead of the viewer's local time. Fix: caller-supplied IANA tz, resolved via IClock + TimeZoneInfo.ConvertTimeFromUtc; required/validated only when a current-time day exists.

Findings (all applied):
1. [Route A · grill-then-plan] Design blast-radius not grilled at design time — an eager, always-required tz on an EXISTING endpoint regressed normal itinerary reads and broadened the MCP get_itinerary contract for every trip; only /scrutinize caught it, costing a whole extra task cycle. Added a "Blast radius of a new requirement" bullet to Step 3 (scope a new requirement to where it is consumed; validate lazily). dev-workflows 0.23.3 → 0.24.0.
2. [Route D · memory] mobile-app write-guard drive-letter case flipped WITHIN one session (lowercase worked early; capital required later — 3 failed Edits). Updated reference_exact_case_paths_write_guard.
3. [Route D · memory] superpowers SDD task-brief / review-package print "wrote <path>: …" to stdout, not a bare path — do not capture as a path. Updated reference_sdd_task_brief_numbering.

Discarded (no durable lesson): the process-skill-over-Ultracode call (already covered by existing memory; worked); the debug→grill→SDD→scrutinize flow itself (no friction).

## 2026-07-13 — menunest — Trip Stop "Review link" (#33)
Flow: grill-then-plan → writing-plans → subagent-driven-development (10 tasks + 3 fix commits) → final review → scrutinize → reflect.
- [Route F · GOTCHAS.md] mobile-app write-guard blocks Edit/Write (outside + case-flip inside project root) while that plugin is connected — subagents must use Bash/PowerShell. APPLIED (created ~/.claude/GOTCHAS.md + @import in global CLAUDE.md).
- [Route D · memory ef-relational-testing-sqlite] EF InMemory doesn't faithfully run ValueConverters and skips ConvertFromProvider on DB NULL → null reads hidden; use SqliteAppDbContext for converter/JSON/NOT-NULL tests. APPLIED. (Hid a critical #33 null-read NRE; caught only by an added relational test.)
- [Route C · menunest CLAUDE.md] Full-suite pre-commit ⇒ every commit must be whole-suite-green; an EF entity/property + its mapping must land in ONE commit (unmapped nav fails model validation for the whole suite). APPLIED.
- [Route C · menunest CLAUDE.md] Frontend has NO component/visual test harness (vitest node env) — tsc/build/unit can't catch rendering/CSS/DOM bugs; UI changes need interactive verification. APPLIED. (scrutinize caught a popover clipped by overflow:hidden.)
Sources: EF DB-NULL/converter behavior verified empirically this session (relational test reproduced the null read). No external prior-art search needed.

## 2026-07-13 · menunest — current-time-start tracks today's date (#35)
- [Route C · menunest CLAUDE.md] git remote named `main` (not origin) → `gh --repo ThodsaphonSonthiphin/MenuNest`, push `git push main HEAD:main` — APPLIED
- [Route F · GOTCHAS.md] grill-then-plan→SDD leaves design docs (ADR/spec/plan/glossary) uncommitted; narrow-staging excludes them — APPLIED
- [Route F · GOTCHAS.md] superpowers SDD scratch (`task-N-*.md`, `progress.md`) is feature-agnostic → collides/accumulates across features — APPLIED
- Skipped: none. Sources: internal session friction (no external prior art needed — C/F edits, not new-skill candidates).

## 2026-07-13 — menunest · issue #23 Place checklist (grill→plan→SDD→scrutinize, 17 commits)

Findings:
- [Route F · GOTCHAS.md] write-guard bypass corrected: prefer PowerShell WriteAllText; Bash `<<'EOF'` heredocs fail here (EOF quote errors); hook may report the `frontend` subdir as root (blocks backend/docs). — applied (updated existing line)
- [Route F · GOTCHAS.md] AskUserQuestion / tool calls with non-ASCII: pass literal UTF-8, never hand-write `\uXXXX` escapes (InputValidationError ×2). — applied (new line)
- [Route C · menunest CLAUDE.md] backend tests use Moq (not NSubstitute); three `IApplicationDbContext` impls (AppDbContext + SqliteAppDbContext + InMemoryAppDbContext) → new DbSet in all three or CS0535. — applied
- [Route D · memory] feedback_plan_scan_all_callers: grep all interface implementers + record construction sites before finalizing (InMemoryAppDbContext + TripPlaceDto fallout). — applied
Skipped/not-routed: 05b1557 out-of-band commit (already covered by memory + GOTCHAS "Workflow/process" section); trailing-newline nit (cosmetic).
Sources: none external (all local overlap).
## 2026-07-10 — GlassHull #6215 test-data enrichment (generating-test-cases x2, debug-mantra, Workflow)
Findings:
- [Route A · generating-test-cases] Every related entity in Test Data must be a directly-openable TYPE-QUALIFIED ref (Dataverse entityset(guid)), not a bare id — bare GUIDs are ambiguous (contact id read as booking id), cost 3 correction rounds. dev-workflows 0.25.0->0.25.1; needs resync+restart.
- [Route F · GOTCHAS.md] Workflow tool args may arrive as a JSON string -> guard typeof args==='string'?JSON.parse(args):args before pipeline().
- [Route F · GOTCHAS.md] Writing an .xlsx open in Excel on Windows -> PermissionError [Errno 13]; render to -vN or have the user close it.
Applied: all 3. Verified live: 11/11 entity refs resolved (0 failed) via verify_refs.py.

## 2026-07-13 — glasshull (#6215: merged VIN card + per-booking email timeline)

Arc: grill-then-plan → SDD execution (8 tasks, per-task review gates) → whole-branch review → live Task 0 (correlation PROVEN) → /scrutinize (5 fixes) → /reflect.

Findings & routes:
- [Route F · GOTCHAS.md] write-guard `.Replace` bypass sub-traps — prefer a SINGLE-LINE anchor (multi-line silently no-ops on trailing-whitespace / CRLF-vs-LF); the blocked set shifts within a session (`docs/adr` wrote while `docs/superpowers/specs` was blocked). APPLIED (refined existing line).
- [Route D · memory] enum/status `default`-branch regressions are invisible to spec-anchored reviews — a post-VIN card-title regression slipped BOTH the per-task and final whole-branch reviews, caught only by scrutinize. APPLIED (new `enum-default-branch-review-gap`).
- [Route D · memory] reusable SDK `check_*_email_*.py` verifiers (DataverseClient, read-only) for send/reply correlation — appended to `email-activity-quote-linkage-6164`. APPLIED.
- Discarded: subagent-vs-direct for fiddly single-file edits (covered by `prefer-direct-edits-small-changes`); Bash nested-quote failure calling PowerShell (covered by GOTCHAS heredoc note).

Sources: firsthand session observations. No external prior-art search — all findings routed to in-place updates / memories (no new-skill candidate).

## 2026-07-13 — menunest (Place profile library, #37)
- [Route F · GOTCHAS.md] PowerShell 5.1 mangles native-exe args with embedded double-quotes -> --body-file/-F. APPLIED.
- [Route F · GOTCHAS.md] Resumed/parallel session may have partially executed the plan -> git status before writing. APPLIED.
- [Route F · GOTCHAS.md] write-guard case-retry unreliable -> go straight to PowerShell (refined). APPLIED.
- [Route D · memory] CORRECTED: user ALWAYS uses SDD subagents; prior "inline preference" memory was a misclick artifact -> deleted, wrote feedback_always_use_subagents.md.
- Sources: none external (self-observed).
- (round 2, same session) [Route C · menunest CLAUDE.md] prod SQL firewalls by IP → migrations need a temp firewall rule for current IP. APPLIED. · [Route F · skipped by user] PowerShell file-delete guard-blocked → use Bash rm.

## 2026-07-14 - GlassHull Bookings List (#6218 follow-ups)

Project: GlassHull (Obsidian/glasshull/raw/glasshull). Three debug-mantra runs on the Bookings list + HAR / Dataverse / ADO-pipeline forensics (one stale-backend flip-flop, corrected via a token-replayed API call).

Findings (all applied):
- [Route F - GOTCHAS.md] CI `git diff HEAD~1 HEAD` path-filter skips multi-commit pushes -> build/deploy stage skipped, pipeline still green -> "pipeline green" != deployed.
- [Route C - raw/glasshull/CLAUDE.md] Deploy-branch mapping: frontend deploys the dev site from `test`, backend deploys app-glasshull-dev from `main`.
- [Route C - GlassHullPortal/CLAUDE.md] MUI X DataGrid: memoize `columns` (stable ref) or `initialState` orderedFields/columnVisibilityModel silently resets to definition order.
- [Route F - GOTCHAS.md] Absent JSON key != field missing from model when the API omits nulls; verify against a record known to hold a non-null value.

Sources: MUI X v8 docs (column-definition), azure-pipelines.yml + ADO pipeline runs (org Cartagena365/GlassHull), HAR (cargoverse), Dataverse Web API probes.

## 2026-07-14 - glasshull #6245 VIN-reminder automation (grill-then-plan)
- [Route D - no edit] thaihr@ staleness: memory `quote-email-crm-redesign-status` ALREADY documented the 2026-07-10 move to cargoverse.sales@ (queue GUID `fb68e326-` stable). Memory was right; failure was not applying it. Fixed 3 stale thaihr@ refs + a "default 24h"->48h in the session walkthrough. No memory edit (existing content is better).
- [Route A - problem-description] check-walkthrough.py: added a `node --check` JS-syntax gate (regex checks passed a `record's` apostrophe-in-single-quoted-GLOSSARY bug I only caught by a manual node --check). +SKILL.md Common-Mistakes row. dev-workflows 0.25.2->0.25.3 (needs cache resync + restart to load).
- [Route F - GOTCHAS.md] Python print() of non-ASCII on Windows console -> UnicodeEncodeError (cp1252); wrap stdout in utf-8 TextIOWrapper.
- [Route F - GOTCHAS.md] raw space in a Dataverse Web API $filter (urllib) -> "URL can't contain control characters"; urllib.parse.quote the clause.
- Applied: all four. Skipped: none.

## [2026-07-15] reflect | glasshull #6245 VIN-reminder (Dataverse + cloud-flow build)
- [Route F - GOTCHAS.md] Dataverse $filter raw-space error hit twice again; refined the line: SDK filter= encodes, only raw urllib/requests needs quote(). APPLIED.
- [Route D - memory update] scripting-power-automate-flow-clientdata: added the "clone a working flow's clientdata as a template" build technique (built the #6245 13-action flow this way). APPLIED.
- [Route D - new memory] glasshull-portal-rbac-component-recipe: ctg_component is a LOCAL optionset + ctg_portalpermission rows + live int-based read (no DLaB regen, no re-login, 1-min Redis cache). APPLIED.
- Discarded: write-guard-blocks-subdir + big-Bash-heredoc-fail (already in GOTCHAS.md); Thai-language replies (one-off; deliverables stayed English).
## [2026-07-15] reflect #2 | glasshull #6245 VIN-reminder (flow build + filter/select debug)
- [Route F - GOTCHAS.md] Power Automate/Logic Apps WDL has no filter()/select() functions -> use Filter array (Query) / Select actions. APPLIED.
- [Route D - memory] verify-connector-capability-before-build: extended to WDL expression primitives (spike/skeleton before big build). APPLIED.
- [Route D - memory] vin-reminder-automation-status: reminder CTA points to CRM-tracked original email (reply-correlation); flow built via clientdata PATCH + verified live. APPLIED.
- Discarded: big-Bash-heredoc-fail + Write-tool-blocks-scripts-subdir (already in GOTCHAS).

## 2026-07-17 -- menunest (issue #39 "Add Set home path" grill-then-plan)

- [Route D - memory] `feedback-visual-mocks` corrected: UI mockups render as an **Artifact** sourced from the project design language or a **Claude Design** project (DesignSync), per `references/ui-mockup.md`; `docs/mocks/` + Chrome DevTools is now fallback-only. -- APPLIED
- [Route F - GOTCHAS.md] Workflow/process gotcha added: UI mockup inside a grilling skill -> Artifact / Claude Design, not a local `docs/mocks/` HTML + DevTools mock. -- APPLIED (inert until next session/restart)
- [Route A - dev-workflows `ui-mockup.md`] reinforcement paragraph "Do not substitute a personal mock habit for this mechanism"; plugin 0.25.4 -> 0.25.5 in plugin.json + marketplace.json. -- APPLIED (needs cache resync + Claude Code restart to load)

Trigger: at grill-then-plan Step 3.5 I defaulted to the stale `docs/mocks/` + Chrome DevTools memory instead of ui-mockup.md's Artifact/Claude Design mechanism; user corrected twice ("ตามสกิลมันให้ใช้ claude design", then /reflect). Root cause = stale project memory outranking a correct owned-skill reference. No external prior art needed (Pass B/C skipped).
## 2026-07-19 -- menunest (issue #40 grill-then-plan)

- [Route F * GOTCHAS.md] DesignSync: a NEW card stays invisible until added to `_ds_manifest.json` (`@dsCard` marker not auto-compiled on push). APPLIED.
- [Route D * memory] Corrected reference-menunest-claude-design-project line ("marker suffices / register_assets not needed") with the manifest caveat. APPLIED.
- [Route A * dev-workflows/references/ui-mockup.md] Added new-card manifest caveat to the "Push via the two-step plan" bullet; bumped dev-workflows 0.25.6 -> 0.25.7. APPLIED (needs cache resync + restart to load).

Sources: none (internal DesignSync behaviour observed live this session).

## 2026-07-20 — menunest #40 (UV/feels + weather alerts): SDD execution -> review -> ship
- [Route D · feedback_plan_scan_all_callers] Field added to the domain `WeatherReading` + frontend DTO but DROPPED at the Application `WeatherReadingDto`/`GetStopWeatherHandler` projection -> `GET /weather` + MCP `get_stop_weather` omitted it -> feature dead against real data. All 6 per-task diff-scoped reviews passed; only the final whole-branch review (Opus) + `/scrutinize` end-to-end trace caught it. APPLIED (extended memory).
- [Route D · project_menunest_parallel_sessions] menunest worked by concurrent sessions on `main` + feature worktrees; live state shifted under the session (#39 SettingsPage prebuilt, prod migration pre-applied, remote diverged -> fetch+rebase before push). APPLIED (new memory + MEMORY.md pointer).
- [Route F · GOTCHAS.md] GitKraken `gk` AI hook silently rewrote a `git commit -m` message into a false `feat:` claim dropping the ticket ref; `--amend` + subagent commits not rewritten. APPLIED.
- Skipped (already covered): write-guard-on-frontend (GOTCHAS + reference_exact_case_paths_write_guard); smoke-before-push (existing menunest CLAUDE.md rule -- user's informed override to push first).
Sources: this session -- SDD (6 tasks, 2 fix loops) + final review + /scrutinize x2 + prod migration + rebase-and-push.

## 2026-07-20 - menunest (#44 Discover review-links + note)
- [Route F - GOTCHAS.md] CSS custom properties don't resolve in a `document.body`-portaled node (Syncfusion Dialog) when the token is page-scoped -> add a `:root` fallback or hardcode in-scope. APPLIED. Evidence: commit b0b7644; trips-tokens.css `:root` fallback comment; TripDetailPage.css `.sd-note-body`.
- Session otherwise clean: /scrutinize on #44 = ship (1 Minor MCP-description doc-drift fixed in 946d5d7 + pushed); ff push 9948762..946d5d7 to main. No other findings cleared the bar.

## 2026-07-21 — menunest · issue #41 (version on API+SPA) · grill-then-plan + scrutinize

- [Route C · CLAUDE.md] Backend test-project map — project CLAUDE.md named only `MenuNest.Application.UnitTests`; four test projects actually exist (+`Infrastructure.IntegrationTests`, `McpServer.UnitTests`, `WebApi.UnitTests`). My spec draft hedged about where to unit-test a WebApi helper; scrutinize flagged it (Major). APPLIED.
- [Route F · GOTCHAS.md] `EnterWorktree` default `worktree.baseRef: fresh` branches from `origin/<default>`, silently omitting unpushed local-`main` commits → lost work + ADR-number collision. Caught proactively (set `baseRef: head`). Fix = set head before EnterWorktree when local main is ahead. APPLIED (inert until restart).
- Skipped: write-guard drive-letter-case recurrence — already covered by existing GOTCHAS line + `reference_exact_case_paths_write_guard` memory.
- Sources: `EnterWorktree` tool contract (baseRef fresh/head semantics); `find backend/tests` → 4 `.csproj`.
## 2026-07-21 - glasshull (RBAC E2E-test planning)
_Analysis limited to post-compaction context._
- [Route D memory] check-project-docs-before-investigating - grep repo docs/ before spawning Explore/probe scripts (rejected subagent + script; user pointed to manual-create-portal-user.md). APPLIED.
- [Route D memory] glasshull-portal-onboarding-flow - no self-service reg; admin Contact -> Sync To B2C -> assign role; delete Contact does not delete B2C; token-perm claim unverified vs live-read. APPLIED.
- Sources: docs/manual-create-portal-user.md (v1.3, 2026-05-18).
## 2026-07-21 - glasshull (mgmt manual + flow-diagram correction)
_Analysis limited to post-compaction context._
- [Route D memory] flow-diagram-arrows-are-claims - drew customer replies as a direct customer->agent email in the mgmt user-manual; real flow routes replies through the tracked mailbox into the portal timeline (#6164). Verified outbound emails but not the inbound arrows; missed own memory. APPLIED.
- Follow-up: corrected the misleading "relays it to the agent" wording in docs/booking-flow-roles-and-permissions.md. APPLIED.
- Sources: email-activity-quote-linkage-6164 memory; CargoDetailService.cs:582-736.
## 2026-07-21 - menunest - issue #46 (weather-based retiming) SDD + scrutinize + fix-all
_Analysis limited to post-compaction context._
- [Route F - GOTCHAS.md] A fresh git worktree / EnterWorktree has NO restored packages (node_modules, NuGet obj/project.assets.json, bin/ git-ignored) -> first build fails (dotnet build NETSDK1004; frontend missing-module); run dotnet restore + npm ci in the worktree before the first build and before dispatching SDD implementers. APPLIED. Evidence: first docs commit in worktree issue-46-hourly-temperature failed NETSDK1004 until restore+npm ci.
- Discarded (process worked as designed): tsc --noEmit vs npm run build (T6 implementer self-caught the unused local on the real build; menunest CLAUDE.md already mandates npm run build); T1 InvariantCulture URL + Fix-BE2 dwell=0 (one-off bugs caught by review/fix loop); H1 verify-before-fix (covered by feedback_check_dont_guess).
- Sources: this session - SDD (9 tasks, 3 fix loops) + /scrutinize (5 tracers) + fix-all; git worktree package-restore behaviour observed live.
## 2026-07-21 - menunest #46 (mockup-vs-prod divergence: why + fix)
- [Route A - dev-workflows/scrutinize] Behaviour/spec review gates (SDD per-task, whole-branch, /scrutinize) are BLIND to visual fidelity -- #46 HourlyPlanner shipped flat/placeholder-styled through all gates + /scrutinize to prod, diverging from its approved Claude Design mock. Added a "Does it match the mock (UI only)?" bullet to step 3 Verify: fetch the mock + statically diff produced markup/CSS (tokens, colors, structural treatment). dev-workflows 0.25.7 -> 0.25.8 (needs cache resync + restart). APPLIED.
- [Route C - menunest CLAUDE.md] Strengthened the "no visual test harness" section: the review gates verify behaviour/spec, NOT visual fidelity; a mockup-backed UI task must be diffed against the mock (DesignSync get_file screens/<name>.html) and/or interactively verified before merge. Captures the DesignSync get_file "expected side" oracle used to debug this. APPLIED.
- [Route F - GOTCHAS.md] non-ASCII (em/en-dash) inlined in a PowerShell @'...'@ here-string can break PS tokenization (spurious Remove-Item parse error) -> Write scripts to a file and run them; don't inline non-ASCII/multi-line content. Refined the existing PowerShell 5.1 line. APPLIED (inert until restart).
- Contributing cause (not routed -- already covered): interactive verify was skipped when a parallel session merged+pushed #46 (project_menunest_parallel_sessions + verify-before-push).
- Sources: this session -- debug-mantra on the color mismatch, DesignSync get_file of the #46 mock vs the deployed CSS, full restyle (commit 8bcccea).

## 2026-07-22 - glasshull (Obsidian raw/glasshull) - RBAC demo/E2E prep
Trigger: /reflect on a live-check miss during grill-with-docs.
- [Route D - memory] APPLIED: parroted stale RBAC counts from 1-day-old status memory; user said "live check again"; live proved drift. Updated rbac-consolidation-3-roles-status.md -> SNAPSHOT flag + LIVE DRIFT block (6 assignments, Apirut also Admin, Operator still 0 users, Amelie+Ford roleless).
- [Route C/F] SKIPPED (redundant): live-check-CRM rule already in project CLAUDE.md; memory-recall already prints a point-in-time disclaimer.
Source: live scripts/rbac_live_check.py vs glasshulldemo.

## 2026-07-24 - GlassHull modularization (grill-then-plan)

Project: glasshull (productize as sellable modules: Core / Quote / Schedule / Booking).
Session: grill-then-plan design -> 10 ADRs (0186-0195) + design spec + adversarial spec-claim verification (10 Explore agents, confidence HIGH, no claim refuted).

Findings:
- [Route F - GOTCHAS.md] Cross-workspace markdown links dead-click: a workspace-relative href opens only if the file is under the ACTIVE VSCode workspace root; linking glasshull docs from the menunest workspace broke every link. Fix: plain absolute paths for out-of-workspace files. APPLIED.
- [Route A - dev-workflows/grill-then-plan] Added Step 5a: verify load-bearing technical claims against code/live BEFORE spec approval. A prose self-review missed a false premise ("Quote's voyage use is only a degradable acceptance-email line"); adversarial code-verification caught it as a HARD send-gate (ADR 0081) that would silently break the "Quote sold standalone" plan. APPLIED (plugin 0.25.9 -> 0.25.10; restart required to load).

Skipped: (a) self-corrected premise "Booking born only from a Won Quote" - glasshull CLAUDE.md already says code-wins and it was verified in-session; (b) ASCII-only in PowerShell here-strings - already in GOTCHAS.

Sources: in-session adversarial verification workflow (10 Explore agents) over the glasshull codebase.

## 2026-07-31 -- glasshull modularization (VC split, session 65cb0214)

FE app-code decoupling executed via subagent-driven-development (Plan-A 9 tasks A2-A13 + cross-module audit + A14 closure); monorepo (0217) + voyage (0218 -> Option B) decisions taken mid-run.

Findings:
- [Route D - memory] Status updates too dense -> user overwhelmed; keep long-run status plain/low-density. APPLIED (new memory communication-density-long-runs).
- [Route F - GOTCHAS.md] Sustained 529 storm -> self-implement verbatim tasks instead of re-gambling subagents. APPLIED.
- [Route F - GOTCHAS.md] `python` via the Bash tool is Windows Python -> use C:/ not /c/ paths. APPLIED.
- [Route D - memory] Cross-module VALUE-edge audit before a carve (type-focused plans miss hook edges). APPLIED (modularization-productization-status).
- [Route F - GOTCHAS.md] git-during-folder-drop gate deletes tracked files -> plain mv + unconditional restore. APPLIED earlier in-session.

Sources: none external (in-session tooling/process lessons).

## 2026-07-31 -- session 65cb0214 (glasshull / crg voyage flip Option B)

Flip: voyage read + grid + commission + email preview -> Schedule module; Core+Quote hides voyages and sends via Print-PDF. Gates green (tsc/vite/vitest 377/probe); scrutinized; pending user manual test.
- [Route F - GOTCHAS.md] EOL varies per-file within one repo -- detect EOL per file for PS anchor edits (QuoteDetail.tsx LF vs CRLF neighbours; one failed batch). APPLIED (rev of write-guard line).
- [Route D - memory] Core+X folder-drop probe must drop ALL non-Core/non-X modules or pre-existing edges masquerade as failures; tsc -b --force; mv-out + unconditional-mv-back. APPLIED (new crg-modularization-verification-notes).
- [Route D - memory] crg vitest mis-collects e2e/*.spec.ts as failed FILES -- read failed-TESTS count. APPLIED (same memory).
- [Route D - memory] UiSlot extraction tsc-invisible for slot-ids + prop names -- verify by grep + registration smoke test. APPLIED (same memory).
Sources: this session's own tsc/vitest/probe output (no external research).