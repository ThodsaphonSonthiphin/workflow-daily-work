# .NET build output (from running create-backlog.cs)
bin/
obj/
.vs/
*.user

# runtime working files produced by a run (not part of the plugin)
findings.json
backlog_input.json
backlog_result.json

# picture-record.jsonl is meant to be committed in a USER's project repo (ADR 0142) -
# this ignore exists only so it can never land inside this marketplace repo itself,
# which installs onto other people's machines
picture-record.jsonl

# ticket-trace test workspace — fixture repos with embedded .git dirs; never commit
plugins/dev-workflows/skills/ticket-trace-workspace/

# Playwright MCP scratch (browser snapshots/logs)
.playwright-mcp/

# harness scratch: git worktrees created by the agent, and subagent-driven-development
# ledgers/briefs/review packages — working files, never part of the marketplace
.claude/worktrees/
.superpowers/

# Python bytecode (was tracked by mistake)
__pycache__/
*.pyc

# --- added by offboarding rescue: never publish local secrets/bulk ---
.env
.env.*
!.env.example
appsettings.Development.json
appsettings.Production.json
**/.local-publish/
**/.azure-migration-backup/
*.pfx
*.pem
*.key
node_modules/
bin/
obj/
.venv/
dist/
build/
