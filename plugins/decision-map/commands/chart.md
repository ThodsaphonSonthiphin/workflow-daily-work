---
description: Chart a Decision map for an effort too big for one session — name the destination, grill breadth-first, create the map and its decision tickets (dry-run gated) as Markdown under docs/decision-map/ or as GitHub issues + sub-issues, fire the research subagents, stop.
argument-hint: "<loose idea — e.g. 'migrate billing to the new provider'>"
---

Use the **`chart-map`** skill to chart a Decision map for: $ARGUMENTS
