---
description: Compact this conversation into a handoff document for another agent — a new harness, a Claude Code cloud session, another machine, a colleague, or a forked side task. Say `cloud` to commit and push it for claude.ai/code.
argument-hint: "[cloud] what will the next session be used for?"
---

Use the **`handoff`** skill. Write the handoff document for the destination the argument names (`cloud` = commit and push into `docs/superpowers/handoffs/`; otherwise the OS temp directory), and print where it landed.

Argument: $ARGUMENTS
