## 2026-07-06 — menunest Trip weather (GitHub #10)

Arc: grill-then-plan → ADRs/CONTEXT/spec (Workflow fan-out + adversarial verify) → writing-plans
(11-task TDD plan, verify pass caught a blocker) → subagent-driven-development (11 tasks, per-task
review, final whole-branch review found 1 cross-cutting cache bug) → finishing-a-development-branch.

Findings (4, all applied):
1. [D] Stale-memory guess — recommended Open-Meteo from a stale "Google billing OFF" memory; user
   said "check", a 1-line live probe showed billing ON + Weather API working → flipped provider to
   Google. Broadened feedback_check_dont_guess beyond Azure to any mutable external state; "stale
   memory = re-verify trigger, not fact".
2. [C] Undocumented full-suite pre-commit hook (frontend/.husky/pre-commit, ~40s) + dirty
   daily-state.md(tracked)/AGENTS.md(untracked) → scoped-add boilerplate repeated in ~13 dispatches.
   Added a "Committing" section to menunest CLAUDE.md.
3. [D] superpowers SDD scripts/task-brief matches only `Task <number>`; B1/F1-labelled plan needed a
   custom awk for all 11 briefs. New memory reference_sdd_task_brief_numbering.
4. [D] mobile-app plugin write-guard (validate-write-safety.js) blocks Write/Edit outside the project
   root & on lowercase `c:\` paths — hit by 2 subagents (F4/F5) and again by this reflect's own
   out-of-root memory write (fixed via Bash). New memory reference_exact_case_paths_write_guard.

Applied: 1,2,3,4. Skipped: none. External prior-art search: none (all local-routing, no new-skill
candidates). Sources: this session only.

## 2026-07-10 — glasshull (Rate Quotation logo removal, /debug-mantra → test cases)

- **F1 (Route C, APPLIED):** Editing a Dynamics SSRS report via `report.bodytext` PATCH updates metadata but does NOT redeploy the compiled copy SSRS renders from — declared "verified live" prematurely; caused a full second debug round. Precedent was already in-repo (`tmp/ctg_swb_ribbon_FIX_NOTES.md`). → added SSRS-report rule + "verify the rendered PDF, not the metadata" to `raw/glasshull/CLAUDE.md` Dataverse section.
- **F2 (Route A, APPLIED):** Handed over test data in the wrong record-state (Draft/Won) for an Active-only button (`ctg_isQuoteActive`) — concrete GUIDs, unusable state. → `generating-test-cases` Step 5 + Red-flag now require the instance to satisfy the entry-point's preconditions (state/enable-rule/permission), verified live. dev-workflows 0.23.1→0.23.2 (needs cache resync + restart).
- **F3 (Route D, APPLIED):** `wiki-write-guard-blocks-vault` memory said the guard only blocks paths outside raw/glasshull; it also misfires for in-root paths (`scripts/`, `docs/qa/`). → corrected the memory: treat Write/Edit as unreliable workspace-wide, use Bash.
- Skipped: none.
- Sources: in-repo SWB fix notes/scripts; MS Learn (report entity / RDL sandboxing / SSRS caching) via the diagnostic workflow.

## 2026-07-16 -- menunest (Trips #36)

Session: /debug-mantra on "add new place -> black map screen"; found + fixed; then /scrutinize + /reflect.

Findings:
- [Route F - GOTCHAS.md] Broad `.map > div { height:100% }` stretches abs-positioned overlay siblings (AddPlaceMode banner) to full map height -> looked like a black map. + @vis.gl <Map> inline-sizes only when NO className. APPLIED.
- [Route D - memory] Updated project_trip_add_new_place_from_itinerary: interactive verify found the black-map bug; fixed 9b5b255 (pushed), verified on prod. APPLIED.
- [Route C - menunest CLAUDE.md] Smoke-test map/overlay UI interactively BEFORE pushing (prod deploys on push). APPLIED.

Root cause: `.add-capture-banner` (a `.trip-map > div` Fragment-sibling of <Map>) inherited height:100% from the over-broad rule; fix scopes to `> div:first-child`. Map itself fine (14 tiles, App Insights clean). Diagnosis: console `elementFromPoint` (hit = add-capture-banner, bg --ink) + deterministic CSS mock + @vis.gl source read.

## 2026-07-22 glasshull -- Quote email timeline redesign (grill-then-plan)
- [Route F - GOTCHAS.md] UI-mockup medium: defaulted to a local `.html` mockup (anchored on an in-repo `docs/superpowers/specs/*.html` precedent) instead of Claude Design via DesignSync. The 2026-07-17 gotcha did not catch it because its negative example named only `docs/mocks/`. Tightened to cover ANY local `.html` path + "even when a repo precedent exists". APPLIED.
- [Route A - dev-workflows grill-then-plan] Step 3.5 one-line pointer named only the `.html` fallback, reinforcing a fallback-first read. Rewrote it to name Claude Design/DesignSync as PREFERRED, `.html` as last resort. APPLIED (plugin 0.25.8 -> 0.25.9; inert until cache resync + restart).
- Root: never opened references/ui-mockup.md this session; anchored on a stale local artifact instead of the documented preference order.
- Source: references/ui-mockup.md (the skill's own reference).